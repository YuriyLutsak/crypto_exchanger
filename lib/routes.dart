

import 'package:crypto_exchanger/features/view/list_screen.dart';

final routes = {
  '/': (context) => CryptoListScreen(),
  // 'coin': (context, coin) => CryptoCoinScreen(coin: coin),
  // 'coinDetails' : (context, coinDetails) => CryptoCoinScreenDetails(coinDetails: coinDetails)
};
